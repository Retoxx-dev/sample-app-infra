updateStrategy:
  type: RollingUpdate

config:
  registries:
    - name: ACR
      api_url: https://acrsampleappneu.azurecr.io
      ping: yes
      credentials: pullsecret:argocd/argocd-image-updater-acr-creds